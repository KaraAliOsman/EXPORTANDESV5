---
import { url } from "@/utils/paths";
const steps = [
  { num: "01", title: "Cotiza", desc: "Envianos un WhatsApp con el tipo y cantidad de material. Respuesta en menos de 2 horas.", color: "bg-blue-600" },
  { num: "02", title: "Retiro", desc: "Coordinamos retiro a domicilio gratis, o traelo a nuestra sede.", color: "bg-blue-500" },
  { num: "03", title: "Pesaje", desc: "Pesamos con balanza certificada. Proceso transparente frente a ti.", color: "bg-blue-400" },
  { num: "04", title: "Pago", desc: "Transferencia o efectivo al instante. Sin demoras ni tramites.", color: "bg-green-500" },
];
---

<section class="py-16 sm:py-24 bg-gray-50">
  <div class="max-w-7xl mx-auto px-5 sm:px-8">
    <div class="text-center mb-12">
      <p class="text-sm font-semibold text-blue-600 mb-1">Proceso simple</p>
      <h2 class="text-3xl sm:text-4xl font-extrabold text-gray-900">Vender en 4 pasos</h2>
      <p class="mt-3 text-gray-500 max-w-md mx-auto">De tu chatarra a dinero en el mismo dia.</p>
    </div>

    <div class="grid grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-8">
      {steps.map((s) => (
        <div class="relative bg-white rounded-2xl border border-gray-100 p-5 sm:p-6 shadow-sm">
          <div class:list={["w-10 h-10 rounded-lg flex items-center justify-center text-white font-bold text-sm mb-4", s.color]}>
            {s.num}
          </div>
          <h3 class="font-bold text-gray-900 mb-1.5 text-sm sm:text-base">{s.title}</h3>
          <p class="text-xs sm:text-sm text-gray-500 leading-relaxed">{s.desc}</p>
        </div>
      ))}
    </div>

    <div class="text-center mt-10">
      <a href={url("vender")} class="inline-flex items-center gap-2 px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg transition-colors shadow-lg shadow-blue-600/20">
        Empezar ahora
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3"/></svg>
      </a>
    </div>
  </div>
</section>
