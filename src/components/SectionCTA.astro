---
import { createGeneralWhatsAppLink } from "@/utils/whatsapp";
import { url } from "@/utils/paths";
const waLink = createGeneralWhatsAppLink();
---

<section class="relative overflow-hidden">
  <div class="absolute inset-0">
    <img src="https://images.unsplash.com/photo-1587293852726-70cdb56c2866?w=1920&q=70&auto=format" alt="" class="w-full h-full object-cover" loading="lazy" />
    <div class="absolute inset-0 bg-[#0a0a0a]/75"></div>
  </div>

  <div class="relative max-w-7xl mx-auto px-5 sm:px-8 py-20 sm:py-32">
    <div class="max-w-xl">
      <h2 class="font-sans font-black text-3xl sm:text-5xl md:text-6xl text-white leading-[1.05]">
        ¿Tienes chatarra para vender?
      </h2>
      <p class="mt-5 sm:mt-6 text-base sm:text-lg text-white/50 max-w-md leading-relaxed">
        Cotiza en menos de 2 minutos. Respondemos por WhatsApp.
      </p>
      <div class="mt-8 sm:mt-10 flex flex-wrap gap-4">
        <a href={url("vender")} class="inline-flex items-center gap-3 px-7 py-3.5 bg-white text-black font-semibold text-sm uppercase tracking-wide hover:bg-white/90 transition-colors">
          Cotizar ahora
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M4.5 19.5l15-15m0 0H8.25m11.25 0v11.25" /></svg>
        </a>
        <a href={waLink} target="_blank" rel="noopener noreferrer"
           class="inline-flex items-center gap-2 px-7 py-3.5 border border-white/20 text-white/80 text-sm uppercase tracking-wide hover:bg-white/5 transition-colors">
          WhatsApp directo
        </a>
      </div>
    </div>
  </div>
</section>
