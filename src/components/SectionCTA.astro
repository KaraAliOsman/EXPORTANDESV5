---
import { createGeneralWhatsAppLink } from "@/utils/whatsapp";
import { url } from "@/utils/paths";
const waLink = createGeneralWhatsAppLink();
---

<section class="relative overflow-hidden">
  <div class="absolute inset-0">
    <img src="https://images.unsplash.com/photo-1504328345606-18bbc8c9d7d1?w=1920&q=70&auto=format" alt="" class="w-full h-full object-cover" loading="lazy"/>
    <div class="absolute inset-0 bg-gradient-to-r from-brand-dark/90 via-brand-navy/85 to-brand-navy/70"></div>
  </div>
  <div class="relative max-w-7xl mx-auto px-5 sm:px-8 py-16 sm:py-24 text-center">
    <h2 class="text-3xl sm:text-4xl font-extrabold text-white mb-4">¿Tienes chatarra para vender?</h2>
    <p class="text-lg text-white/60 max-w-lg mx-auto mb-8">Cotiza en menos de 2 minutos. Respondemos por WhatsApp en horario habil.</p>
    <div class="flex flex-wrap justify-center gap-3">
      <a href={url("vender")} class="inline-flex items-center gap-2 px-6 py-3 bg-brand-teal hover:bg-brand-teal/90 text-white font-semibold rounded-lg transition-colors shadow-lg shadow-brand-teal/25">
        Cotizar ahora
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3"/></svg>
      </a>
      <a href={waLink} target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 px-6 py-3 border border-white/25 text-white font-semibold rounded-lg hover:bg-white/10 transition-colors">
        WhatsApp directo
      </a>
    </div>
  </div>
</section>
